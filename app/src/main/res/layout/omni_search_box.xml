<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent" android:layout_height="match_parent">

    <ImageButton
        android:layout_width="60dp"
        android:layout_height="fill_parent"
        android:id="@+id/omni_button"
        android:layout_alignParentRight="true"
        android:layout_alignParentBottom="true"
        android:layout_alignParentEnd="true"
        android:src="@drawable/abc_ic_go_search_api_holo_light" />

    <AutoCompleteTextView
        android:layout_width="wrap_content"
        android:layout_height="fill_parent"
        android:id="@+id/omni_text"
        android:inputType="text"
        android:imeOptions="actionDone"
        android:layout_alignParentLeft="true"
        android:layout_alignParentStart="true"
        android:layout_alignBottom="@+id/omni_button"
        android:layout_toLeftOf="@+id/omni_button"
        android:layout_toStartOf="@+id/omni_button"
        android:hint="Stop name"
        android:dropDownWidth="match_parent"
        android:dropDownHorizontalOffset="-40dp"/>

    <Button
        android:id="@+id/omni_clear_button"
        android:layout_width="40dp"
        android:layout_height="40dp"
        android:layout_gravity="right|center_vertical"
        android:background="@drawable/abc_ic_clear_holo_light"
        android:layout_alignBaseline="@+id/omni_text"
        android:layout_toLeftOf="@+id/omni_button"
        android:layout_toStartOf="@+id/omni_button" />

</RelativeLayout>