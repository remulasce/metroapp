This is the directory where the created routelines databases live.

The dbs are like 500MB each. So I've added them to .gitignore.

The generator is in the directory above this.

This readme keeps this dir open in git.